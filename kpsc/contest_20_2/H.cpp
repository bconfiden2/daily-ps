#include <iostream>

using namespace std;

int n;
int coal[500000];
bool able;

int main(void)
{
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    cin >> n;
    for(int i = 0 ; i < n ; i++)
    {
        cin >> coal[i];
    }

    for(int i = 0 ; i < n ; i++)
    {
        
    }

    cout << (able ? "YES" : "NO") << endl;
}